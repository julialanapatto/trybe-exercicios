//Faça um programa que retorne o maior de dois números. Defina no começo do programa duas constantes com os valores que serão comparados.
//Make a program that returns the greater of two numbers. At the beginning of the program, define two constants with the values ​​that will be compared.
const firstNumber = 45
const secondNumber = 40
if (firstNumber>secondNumber) {
    console.log ("O primeiro número é o maior / The first number is the biggest")
}
else {
    console.log ("O segundo número é o maior / The second number is the biggest")
};